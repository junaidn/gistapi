import { COLORS } from "../../utils/colors";

export const iconButtonStyles = {
  content: {
    display: "flex",
    gap: "4px",
    alignItems: "center",
    color: COLORS.blue,
    fontWeight: "bold",
  },
};
